#!/usr/bin/env bash
# A .python-version file holds one bare version per line. Take the first.
set -uo pipefail
file="${1:?usage: parse-legacy-file <file>}"
[ -f "$file" ] || exit 0
grep -v -E '^\s*(#|$)' "$file" | head -n1 | tr -d '[:space:]'
